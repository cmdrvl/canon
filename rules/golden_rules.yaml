strategy_id: canon-v1-operator-journey.v1
strategy_version: "0.1.0"
entity_type: organization
identity:
  reference:
    id_columns: [source_row_id]
  target:
    id_columns: [source_row_id]
candidate_filter:
  - field_ref: bucket
    field_tgt: bucket
    op: exact
assertions:
  - field_ref: org_name
    field_tgt: org_name
    op: prefix
    weight: 1.0
    required: false
match_threshold: 0.75
ambiguity_gap: 0.10
max_candidates: 10
